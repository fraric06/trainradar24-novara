process.env.TZ='Europe/Rome';
const fs=require('fs'),path=require('path'),https=require('https');
const DATA=path.join(__dirname,'data'),CACHE=path.join(DATA,'piemonte-realtime.json');
const BASE='https://www.viaggiatreno.it/infomobilita/resteasy/viaggiatreno';
let stations=[],live=new Map(),lastScan=0,lastError=null,scanPromise=null;
const request=(p,raw=false)=>new Promise((resolve,reject)=>{const u=new URL(BASE+p),r=https.get(u,{headers:{'User-Agent':'TrainRadar24-Piemonte/3.0','Accept':'application/json,text/plain,*/*','Connection':'close'}},res=>{let b='';res.setEncoding('utf8');res.on('data',c=>b+=c);res.on('end',()=>{if(res.statusCode===204)return resolve(null);if(res.statusCode<200||res.statusCode>=300)return reject(Error('HTTP '+res.statusCode));if(raw)return resolve(b);try{resolve(b?JSON.parse(b):null)}catch(e){reject(e)}})});r.setTimeout(12000,()=>r.destroy(Error('timeout')));r.on('error',reject)});
const num=x=>String(x?.numeroTreno??x?.compNumeroTreno??x?.trainNumber??'').replace(/\D/g,'');
const delay=x=>{const n=Number(x);return Number.isFinite(n)?Math.max(0,Math.round(n)):0};
const category=x=>{const s=`${x?.categoria||''} ${x?.categoriaDescrizione||''} ${x?.compTipologiaTreno||''}`.toUpperCase(),c=Number(x?.codiceCliente);if(c===1||/FRECCIA|\bAV\b|\bFR\b|\bFA\b|\bFB\b/.test(s))return'AV';if(c===4||/INTERCITY|\bICN?\b/.test(s))return'IC';if(/REGIONALE VELOCE|\bRV\b/.test(s))return'RV';return'REG'};
async function loadStations(){const a=await request('/elencoStazioni/3');stations=(Array.isArray(a)?a:[]).map(s=>({code:s.codiceStazione||s.codStazione||s.localita?.id,name:s.localita?.nomeLungo||s.localita?.nomeBreve||s.localita?.label||s.nomeCitta||s.codiceStazione,lat:Number(s.lat),lon:Number(s.lon)})).filter(s=>/^S\d{5}$/.test(s.code)&&Number.isFinite(s.lat)&&Number.isFinite(s.lon));return stations.length}
function station(code){return stations.find(s=>s.code===code)}
function collectTrainObjects(value,out=[]){if(!value||typeof value!=='object')return out;if(Array.isArray(value)){for(const v of value)collectTrainObjects(v,out);return out}const n=num(value);if(n)out.push(value);for(const v of Object.values(value))if(v&&typeof v==='object')collectTrainObjects(v,out);return out}
function segmentTrain(seg,obj){const a=station(seg.nodoA),b=station(seg.nodoB);const lat=(Number(seg.latitudineA)+Number(seg.latitudineB))/2,lon=(Number(seg.longitudineA)+Number(seg.longitudineB))/2;if(!Number.isFinite(lat)||!Number.isFinite(lon))return null;const n=num(obj);const o=obj?.origine||obj?.stazioneOrigine||a?.name||seg.nodoA;const d=obj?.destinazione||obj?.stazioneDestinazione||b?.name||seg.nodoB;return{trip_id:n?`PIE-MAP-${n}`:`PIE-SEG-${seg.trattaAB}-${seg.trattaBA}`,route:category(obj||{}),route_name:`${o} → ${d}`,category:category(obj||{}),region:'PIE',operator:Number(obj?.codiceCliente)===63?'Trenord':'Trenitalia',origin:o,destination:d,train_number:n||`PIE-${seg.nodoA}-${seg.nodoB}`,delay_min:delay(obj?.ritardo??obj?.ritardoAndamento),delay_known:n?true:false,delay_status:!n?'unknown':delay(obj?.ritardo)<5?'on_time':'delayed',realtime:true,realtime_source:'ViaggiaTreno Piemonte · mappa occupazione',realtime_updated_at:new Date().toISOString(),lat,lon,status:'running',nextStop:b?.name||d,progress:50,lastSeen:Date.now(),map_segment:{a:seg.nodoA,b:seg.nodoB}}}
async function mapScan(){const segments=await request('/elencoTratte/3/8/0/false/0');const occupied=(Array.isArray(segments)?segments:[]).filter(s=>s&&s.occupata);if(!occupied.length)return 0;const found=[];for(let i=0;i<occupied.length;i+=8){const batch=occupied.slice(i,i+8);const results=await Promise.all(batch.map(async seg=>{try{return{seg,d:await request(`/dettagliTratta/3/${encodeURIComponent(seg.trattaAB||0)}/${encodeURIComponent(seg.trattaBA||0)}/0/false`)}}catch{return{seg,d:null}}}));for(const r of results){const objs=collectTrainObjects(r.d);const usable=objs.find(x=>num(x)&&x.circolante!==false)||objs[0];const t=segmentTrain(r.seg,usable||{});if(t)found.push(t)}}const dedup=new Map();for(const t of found)dedup.set(t.trip_id,t);for(const t of dedup.values())live.set(t.trip_id,t);return dedup.size}
async function boardScan(){if(!stations.length)await loadStations();if(!stations.length)return 0;const selected=stations.slice(0,Math.min(36,stations.length));const raw=(await Promise.all(selected.flatMap(s=>[board(s,'partenze'),board(s,'arrivi')]))).flat();const uniq=new Map();for(const x of raw){const n=num(x);if(n)uniq.set(`${n}|${x._board}|${x._station.code}`,x)}let added=0;for(const x of uniq.values()){const s=x._station,n=num(x);const t={trip_id:`PIE-BOARD-${n}-${s.code}-${x._board}`,route:category(x),route_name:`${x?.origine||s.name} → ${x?.destinazione||s.name}`,category:category(x),region:'PIE',operator:Number(x?.codiceCliente)===63?'Trenord':'Trenitalia',origin:x?.origine||s.name,destination:x?.destinazione||s.name,train_number:n,delay_min:delay(x?.ritardo??x?.ritardoPartenza??x?.ritardoArrivo),delay_known:true,realtime:true,realtime_source:'ViaggiaTreno Piemonte · tabellone',realtime_updated_at:new Date().toISOString(),lat:s.lat,lon:s.lon,status:'running',nextStop:x?.destinazione||s.name,progress:0,lastSeen:Date.now()};live.set(t.trip_id,t);added++}return added}
async function scan(){if(!stations.length)await loadStations();const mapFound=await mapScan();const boardFound=await boardScan();const cutoff=Date.now()-3*60000;for(const[k,t]of live)if(!t.lastSeen||t.lastSeen<cutoff)live.delete(k);lastScan=Date.now();lastError=null;fs.mkdirSync(DATA,{recursive:true});fs.writeFileSync(CACHE,JSON.stringify({saved:lastScan,stations:stations.length,trips:[...live.values()]}));console.log(`PIEMONTE realtime: stazioni=${stations.length} segmenti occupati=${mapFound} tabelloni=${boardFound} treni=${live.size}`);return[...live.values()]}
async function load(){if(!scanPromise&&(Date.now()-lastScan>10000||!live.size)){scanPromise=scan().catch(e=>{lastError=e.message;try{const c=JSON.parse(fs.readFileSync(CACHE));live=new Map((c.trips||[]).map(x=>[x.trip_id,x]))}catch{}return[...live.values()]});try{await scanPromise}finally{scanPromise=null}}return{generatedAt:new Date().toISOString(),source:'ViaggiaTreno realtime · Piemonte completo via mappa + stazioni',trips:[...live.values()],status:{stations:stations.length,liveTrains:live.size,lastScan:lastScan?new Date(lastScan).toISOString():null,error:lastError,regionsTotal:1,region:'PIE',scanning:Boolean(scanPromise)}}}
async function board(s,kind){try{const d=await request(`/${kind}/${encodeURIComponent(s.code)}/${encodeURIComponent(new Date().toString())}`);return(Array.isArray(d)?d:[]).map(x=>({...x,_board:kind,_station:s}))}catch{return[]}}
module.exports={load,realtime:load};
