process.env.TZ='Europe/Rome';
const express=require('express'),cors=require('cors'),fs=require('fs'),path=require('path'),http=require('http'),https=require('https');
const app=express();app.use(cors());app.use(express.static(path.join(__dirname,'public')));const PORT=process.env.PORT||3000;
const TRIPS=JSON.parse(fs.readFileSync(path.join(__dirname,'data','trips.json'),'utf8'));
const ROUTE_NAMES={S6:'Novara – Milano Passante – Treviglio',R27:'Novara Nord – Saronno – Milano Cadorna',R25:'Mortara – Novara'};
const STATION_IDS=['S00248','S00023','S00034','S01066','S01037'];
const DISPLAY_WINDOW=120,MAX_VISIBLE=140;
function haversine(a,b,c,d){const R=6371,r=x=>x*Math.PI/180,dl=r(c-a),dn=r(d-b);const x=Math.sin(dl/2)**2+Math.cos(r(a))*Math.cos(r(c))*Math.sin(dn/2)**2;return 2*R*Math.asin(Math.sqrt(x))}
function prepareTrip(t){let dist=0,cum=[0];for(let i=1;i<t.stops.length;i++){dist+=haversine(t.stops[i-1].lat,t.stops[i-1].lon,t.stops[i].lat,t.stops[i].lon);cum.push(dist)}return {...t,cumDist:cum,totalDist:dist}}
const PREPARED_TRIPS=TRIPS.map(prepareTrip);
function romeNow(){const d=new Date();return d.getHours()*60+d.getMinutes()+d.getSeconds()/60}
function romeDate(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function serviceNow(trip,now){const first=Number(trip.stops[0]?.dep_min)||0;return first>=1440&&now<180?now+1440:now}
function tripValidToday(t){if(t.region)return true;const m=t.trip_id?.match(/-(\d{4}-\d{2}-\d{2})-(\d{4}-\d{2}-\d{2})$/);if(!m)return true;const d=romeDate();return d>=m[1]&&d<=m[2]}
function estimatePosition(trip,nowMin,delay=0,window=DISPLAY_WINDOW){const first=trip.stops[0],last=trip.stops.at(-1),dep=first.dep_min+delay,arr=last.arr_min+delay;if(nowMin<dep-window||nowMin>arr+window)return null;if(nowMin<=dep)return{status:'not_departed',lat:first.lat,lon:first.lon,nextStop:first.name,etaMin:Math.max(0,Math.round(dep-nowMin))};if(nowMin>=arr)return{status:'arrived',lat:last.lat,lon:last.lon,nextStop:null,etaMin:0};for(let i=0;i<trip.stops.length-1;i++){const a=trip.stops[i],b=trip.stops[i+1],ad=a.dep_min+delay,ba=b.arr_min+delay;if(nowMin>=ad&&nowMin<=ba){const q=(nowMin-ad)/(ba-ad||1);return{status:'running',lat:a.lat+(b.lat-a.lat)*q,lon:a.lon+(b.lon-a.lon)*q,fromStop:a.name,nextStop:b.name,etaMin:Math.max(0,Math.round(ba-nowMin)),progress:Math.round(q*100)}}}return null}
function category(trip){const x=`${trip.category||''} ${trip.route_name||''} ${trip.route||''}`.toUpperCase();if(x.includes('REGIONALE VELOCE')||/\bRV\b/.test(x)||(/TORINO/.test(x)&&/MILANO/.test(x)))return'RV';if(x.includes('MALPENSA')||x.includes('MXP'))return'MXP';if(x.includes('SFM'))return'SFM';if(/^S\d/.test(String(trip.route||''))||x.includes('SUBURB'))return'SUB';return'REG'}
function scheduledTrainNumber(trip){if(trip.train_number)return String(trip.train_number);const raw=String(trip.trip_id||'').split('-')[0];if(trip.route==='S6'&&/^124\d+$/.test(raw))return raw.slice(1);if(trip.route==='R25'&&/^111\d+$/.test(raw))return raw.slice(1);if(trip.route!=='R27')return null;const dep=trip.stops[0]?.dep_min,origin=trip.stops[0]?.id;const novara={371:'4214',431:'4222',491:'4226',551:'4232',611:'4236',671:'4240',731:'4244',791:'4246',851:'4250',911:'4254',971:'4260',1031:'4266',1091:'4270',1151:'4276',1211:'4280'};const cadorna={407:'4219',467:'4225',527:'4229',587:'4235',647:'4241',707:'4245',767:'4249',827:'4255',887:'4259',947:'4265',1007:'4273',1067:'4279'};if(origin==='S00023')return novara[dep]||null;if(origin==='S01066')return cadorna[dep]||null;return null}
let realtime={available:false,updatedAt:null,records:[],error:null},refreshing=false;
function requestText(url,redirects=0){return new Promise((resolve,reject)=>{const u=new URL(url),client=u.protocol==='http:'?http:https;const req=client.get(u,{headers:{'User-Agent':'TrainRadar24-Novara/1.2','Accept':'application/json,text/plain,*/*'}},res=>{if([301,302,303,307,308].includes(res.statusCode)&&res.headers.location&&redirects<4){res.resume();return resolve(requestText(new URL(res.headers.location,u).toString(),redirects+1))}let body='';res.setEncoding('utf8');res.on('data',c=>body+=c);res.on('end',()=>{if(res.statusCode===204)return resolve('');if(res.statusCode<200||res.statusCode>=300)return reject(new Error(`HTTP ${res.statusCode}`));resolve(body)})});req.setTimeout(15000,()=>req.destroy(new Error('timeout')));req.on('error',reject)})}
async function fetchVT(p){let last=null;for(const base of ['https://www.viaggiatreno.it/infomobilita/resteasy/viaggiatreno','http://www.viaggiatreno.it/infomobilita/resteasy/viaggiatreno']){try{const text=await requestText(base+p);if(!text)return null;return JSON.parse(text)}catch(e){last=e}}throw last||new Error('ViaggiaTreno non raggiungibile')}
function vtDate(){return new Date().toString()}
function minuteFromDate(v){if(v===null||v===undefined)return null;const n=Number(v);if(Number.isFinite(n)){const d=new Date(n);return d.getHours()*60+d.getMinutes()}const m=String(v).match(/(\d{1,2}):(\d{2})/);return m?Number(m[1])*60+Number(m[2]):null}
function parseDelayValue(v){if(v===null||v===undefined)return null;if(typeof v==='number'&&Number.isFinite(v))return Math.max(0,Math.round(v));const s=String(v).replace(/&nbsp;/g,' ').trim();if(!s)return null;if(/in\s*orario|on\s*time|puntuale/i.test(s))return 0;if(/anticipo|early/i.test(s))return 0;const m=s.match(/(?:ritardo(?:\s+di)?|delay(?:\s+of)?)?\s*(\d+(?:[.,]\d+)?)\s*(?:min|minute)/i)||s.match(/(\d+(?:[.,]\d+)?)\s*(?:min|minute)/i);if(!m)return null;const n=Number(m[1].replace(',','.'));return Number.isFinite(n)?Math.max(0,Math.round(n)):null}
function delayFromStop(s){if(!s||typeof s!=='object')return null;for(const key of ['ritardo','ritardoPartenza','ritardoArrivo']){const n=parseDelayValue(s[key]);if(n!==null)return n}const scheduled=Number(s.programmata??s.partenzaTeorica??s.arrivoTeorico),actual=Number(s.effettiva??s.partenzaReale??s.arrivoReale);if(Number.isFinite(scheduled)&&Number.isFinite(actual)&&actual>0)return Math.max(0,Math.round((actual-scheduled)/60000));return null}
function parseVT(item,stationId){const min=minuteFromDate(item?.orarioPartenza??item?.compOrarioPartenza??item?.partenzaTeorica);if(min===null)return null;const number=String(item?.numeroTreno??item?.compNumeroTreno??'').replace(/\D/g,'');if(!number)return null;let delay=null;for(const key of ['ritardo','ritardoPartenza','ritardoArrivo']){delay=parseDelayValue(item?.[key]);if(delay!==null)break}if(delay===null)delay=parseDelayValue(item?.compRitardoAndamento?.[0]);if(delay===null)delay=parseDelayValue(item?.compRitardo?.[0]);if(delay===null)delay=0;return{stationId,number,scheduledMin:min,delayMin:delay,destination:item?.destinazione||item?.destinazioneEstesa||null,cancelled:Number(item?.provvedimento)===1,updatedAt:new Date().toISOString(),source:'partenze'}}
function midnightMillis(){const d=new Date();d.setHours(0,0,0,0);return d.getTime()}
function parseAndamento(data,trip,number){if(!data||typeof data!=='object')return null;let delay=null;for(const key of ['ritardo','ritardoPartenza','ritardoArrivo']){const n=parseDelayValue(data[key]);if(n!==null){delay=n;break}}if(delay===null)for(const key of ['compRitardoAndamento','compRitardo']){const n=parseDelayValue(Array.isArray(data[key])?data[key][0]:data[key]);if(n!==null){delay=n;break}}const stops=Array.isArray(data.fermate)?data.fermate:[];if(delay===null&&stops.length){const current=stops.find(s=>s?.stazioneCorrente===true||s?.stazioneCorrente===1);const vals=(current?[current]:stops).map(delayFromStop).filter(v=>v!==null);if(vals.length)delay=current?vals[0]:Math.max(...vals)}if(delay===null)return null;return{stationId:trip.stops[0].id,number:String(number),scheduledMin:trip.stops[0].dep_min,delayMin:delay,cancelled:['ST','SI','SF'].includes(String(data.tipoTreno||'')),updatedAt:new Date().toISOString(),source:'andamentoTreno'}}
function parseTratteCanvas(data,trip,number){if(!Array.isArray(data)||!data.length)return null;const vals=[];for(const entry of data){const d=delayFromStop(entry?.fermata||entry);if(d!==null)vals.push(d)}if(!vals.length)return null;return{stationId:trip.stops[0].id,number:String(number),scheduledMin:trip.stops[0].dep_min,delayMin:Math.max(...vals),cancelled:false,updatedAt:new Date().toISOString(),source:'tratteCanvas'}}
async function getTrainRealtime(trip,number){const origin=trip.stops[0].id,day=midnightMillis();try{const data=await fetchVT(`/andamentoTreno/${origin}/${number}/${day}`);const x=parseAndamento(data,trip,number);if(x)return x}catch(e){}try{const data=await fetchVT(`/tratteCanvas/${origin}/${number}/${day}`);return parseTratteCanvas(data,trip,number)}catch(e){return null}}
async function refreshRealtime(){if(refreshing)return;refreshing=true;try{const records=[],errors=[],now=romeNow();const date=encodeURIComponent(vtDate());await Promise.all(STATION_IDS.map(async stationId=>{try{const data=await fetchVT(`/partenze/${stationId}/${date}`);if(Array.isArray(data))for(const item of data){const x=parseVT(item,stationId);if(x)records.push(x)}}catch(e){errors.push(`${stationId}: ${e.message}`)}}));const active=PREPARED_TRIPS.filter(t=>!t.region&&tripValidToday(t)&&now>=t.stops[0].dep_min-5&&now<=t.stops.at(-1).arr_min+5);const results=await Promise.all(active.map(async trip=>{const n=scheduledTrainNumber(trip);return n?getTrainRealtime(trip,n):null}));for(const x of results)if(x)records.push(x);realtime={available:records.length>0,updatedAt:new Date().toISOString(),records,error:errors.length?errors.join(' | '):null};console.log(`ViaggiaTreno: ${records.length} records`)}finally{refreshing=false}}
function findRealtime(trip,number){if(number){const exact=realtime.records.find(r=>String(r.number)===String(number));if(exact)return exact}const origin=trip.stops[0]?.id;let best=null;for(const r of realtime.records){if(r.stationId!==origin)continue;const diff=Math.abs(r.scheduledMin-trip.stops[0].dep_min);if(diff<=3&&(!best||diff<best.diff))best={...r,diff}}return best}
function trainObject(trip,now){const n=scheduledTrainNumber(trip),live=findRealtime(trip,n),delay=live?.delayMin??0,adjNow=serviceNow(trip,now),pos=estimatePosition(trip,adjNow,delay,DISPLAY_WINDOW);if(!pos)return null;const displayNumber=n||live?.number||'';return{trip_id:trip.trip_id,route:trip.route,route_name:trip.route_name||ROUTE_NAMES[trip.route]||trip.route,category:category(trip),region:trip.region||'lombardia',dep:trip.dep,arr:trip.arr,origin:trip.stops[0].name,destination:trip.stops.at(-1).name,train_number:displayNumber,delay_min:delay,delay_known:Boolean(live),delay_status:!live?'unknown':delay<5?'on_time':delay<=30?'delayed':'severe_delay',realtime:Boolean(live),realtime_train_number:displayNumber,realtime_source_number:live?.number||null,realtime_source:live?.source||null,realtime_updated_at:live?.updatedAt||realtime.updatedAt,cancelled:live?.cancelled||false,stops:trip.stops,...pos}}
app.get('/api/trains',(req,res)=>{try{const now=romeNow(),items=[];for(const trip of PREPARED_TRIPS){if(!tripValidToday(trip))continue;const x=trainObject(trip,now);if(x)items.push(x)}items.sort((a,b)=>{const ar=a.category==='RV'?0:1,br=b.category==='RV'?0:1;if(ar!==br)return ar-br;const ad=Math.abs((a.stops[0]?.dep_min||0)-now),bd=Math.abs((b.stops[0]?.dep_min||0)-now);return ad-bd});const trains=items.slice(0,MAX_VISIBLE);res.json({generated_at:new Date().toISOString(),reference_minutes:Math.round(now),source:realtime.available?'GTFS + ViaggiaTreno realtime':'GTFS static',realtime_available:realtime.available,realtime_updated_at:realtime.updatedAt,count:trains.length,total_candidates:items.length,trains})}catch(e){console.error('/api/trains',e);res.status(500).json({error:e.message,trains:[]})}});
app.get('/api/network',(req,res)=>{try{const today=PREPARED_TRIPS.filter(t=>tripValidToday(t)),seen=new Set(),routes=[];for(const t of today){const points=t.stops.map(s=>[Number(s.lat),Number(s.lon)]);if(points.length<2)continue;const sig=`${t.region||'local'}|${t.route}|${points.map(p=>p.map(v=>v.toFixed(4)).join(',')).join(';')}`;if(seen.has(sig))continue;seen.add(sig);routes.push({key:sig,route:t.route,route_name:t.route_name||t.route,category:category(t),region:t.region||'lombardia',points})}res.json({generated_at:new Date().toISOString(),total_routes:routes.length,routes})}catch(e){res.status(500).json({error:e.message,routes:[]})}});
app.get('/api/regional-status',(req,res)=>{try{const p=path.join(__dirname,'data','regional-trips.json');if(!fs.existsSync(p))return res.json({available:false,status:{}});const x=JSON.parse(fs.readFileSync(p,'utf8'));res.json({available:true,generated_at:x.generatedAt,status:x.status,trips:Array.isArray(x.trips)?x.trips.length:0})}catch(e){res.status(500).json({available:false,error:e.message})}});
app.get('/api/realtime-status',(req,res)=>res.json({available:realtime.available,updated_at:realtime.updatedAt,record_count:realtime.records.length,error:realtime.error,sample:realtime.records.slice(0,10)}));
app.get('/api/health',(req,res)=>res.json({ok:true,trips:PREPARED_TRIPS.length,date:romeDate(),time:romeNow(),realtime:realtime.available}));
app.get('/api/routes',(req,res)=>res.json(Object.entries(ROUTE_NAMES).map(([id,name])=>({id,name}))));
app.listen(PORT,()=>{console.log(`TrainRadar24 backend attivo su porta ${PORT}`);console.log(`Corse caricate: ${PREPARED_TRIPS.length}`);refreshRealtime().catch(e=>console.error(e));setInterval(()=>refreshRealtime().catch(e=>console.error(e)),30000)});
