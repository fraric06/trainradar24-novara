// TrainRadar24 Piemonte realtime adapter V13.
// ViaggiaTreno remains the source of truth. Keep the position calculated by the
// national loader: last detection is informational and must never freeze the marker
// at the last station reported by ViaggiaTreno.
const base=require('./national-loader-v10');
function endpointName(stop){return String(stop?.name||'').trim()}
function serviceEndpoints(trip){const stops=Array.isArray(trip?.stops)?trip.stops:[];if(!stops.length)return{origin:trip?.origin||'',destination:trip?.destination||''};const first=stops.find(s=>s?.tipoFermata==='P')||stops[0];const last=[...stops].reverse().find(s=>s?.tipoFermata==='A')||stops[stops.length-1];let origin=endpointName(first)||String(trip?.origin||'').trim();let destination=endpointName(last)||String(trip?.destination||'').trim();if(trip?.origin_foreign)origin=String(trip.origin_foreign).trim()||origin;if(trip?.destination_foreign)destination=String(trip.destination_foreign).trim()||destination;const n=Number(trip?.verified_train_number||trip?.train_number);const c=String(trip?.verified_category||trip?.category||'').toUpperCase();if(c==='EC'&&Number.isFinite(n)){const baselToMilan=new Set([61,63,65,67,173]),milanToBasel=new Set([60,62,64,66,178]),milanToFrankfurt=new Set([150]),frankfurtToMilan=new Set([151]);if(!trip?.destination_foreign&&milanToBasel.has(n))destination='BASEL SBB';if(!trip?.origin_foreign&&baselToMilan.has(n))origin='BASEL SBB';if(!trip?.destination_foreign&&milanToFrankfurt.has(n))destination='FRANKFURT(MAIN)HBF';if(!trip?.origin_foreign&&frankfurtToMilan.has(n))origin='FRANKFURT(MAIN)HBF'}return{origin,destination}}
function normalizeCategory(trip){const raw=String(trip?.verified_category||trip?.category||'').toUpperCase().trim();if(raw==='EC'||raw==='EIC'||raw==='IC2'||raw==='EN')return'IC';return raw||'REG'}
function fixTrip(trip){const {origin,destination}=serviceEndpoints(trip);const category=normalizeCategory(trip);return{...trip,origin:origin||trip.origin,destination:destination||trip.destination,category,verified_category:category,service_category:category,category_group:category==='IC'?'IC':trip.category_group,route_name:`${origin||trip.origin} → ${destination||trip.destination}`}}
async function load(){const payload=await base.load();return{...payload,trips:(payload.trips||[]).map(fixTrip)}}
module.exports={...base,load,realtime:load};